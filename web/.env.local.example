# Backend API URL (FastAPI server)
# For local development, the default is http://localhost:8000
# Change this if your backend runs on a different port or host
BACKEND_URL=http://localhost:8000

# Shared secret between Next.js and FastAPI backend.
# When set, only requests from the Next.js server (not direct API calls)
# can create generation jobs. Use any random string.
# Generate one with: openssl rand -hex 32
API_SECRET=
